[build-system]
requires = ["setuptools>=77.0.3"]
build-backend = "setuptools.build_meta"

[project]
name = "yaesm"
version = "0.0.4"
description = "A backup tool with support for multiple filesystems"
authors = [
    {name = "Connor Gallivan"},
    {name = "Nicholas B. Hubbard", email = "nicholashubbard@posteo.net"}
]
maintainers = [
    {name = "Connor Gallivan"},
    {name = "Nicholas B. Hubbard", email = "nicholashubbard@posteo.net"}
]
readme = "README.md"
requires-python = ">=3.10"
license = "GPL-3.0-or-later"
license-files = ["LICENSE"]
keywords = ["backup", "filesystem", "snapshot"]

classifiers = [
    "Intended Audience :: System Administrators",
    "Operating System :: POSIX :: Linux",
    "Programming Language :: Python :: 3",
    "Topic :: System :: Archiving :: Backup",
]

dependencies = [
    "apscheduler>=3.11.0",
    "pyyaml>=6.0.2",
    "tzlocal>=5.0",
    "voluptuous>=0.14.2",
]

[project.scripts]
yaesm = "yaesm.main:main"

[project.optional-dependencies]
test = [
    "pytest>=8.3.4",
    "pytest-cov>=7.0.0",
    "pytest-error-for-skips>=2.0.2",
    "pytest-ruff>=0.4.0",
    "pytest-ty>=0.0.1",
    "freezegun>=1.5.1",
    "hypothesis>=6.0.0",
]
dev = [
    "ruff>=0.15.0",
]

[tool.ruff]
required-version = ">=0.15.0"
line-length = 100

[tool.ruff.lint]
select = ["D", "E4", "E7", "E9", "F", "UP", "B", "SIM", "I", "E501"]
ignore = ["D1", "D203", "D205", "D213", "D401", "D404"]
typing-modules = ["yaesm.ty"]

[tool.ruff.format]
quote-style = "double"
indent-style = "space"
line-ending = "lf"
skip-magic-trailing-comma = false
preview = false
docstring-code-format = true

[project.urls]
Homepage = "https://github.com/Vultimate1/yaesm"
Repository = "https://github.com/Vultimate1/yaesm"
Issues = "https://github.com/Vultimate1/yaesm/issues"

[tool.setuptools]
package-dir = {"" = "src"}

[tool.setuptools.packages.find]
where = ["src"]

[tool.pytest.ini_options]
pythonpath = ["src"]
testpaths = ["src", "tests"]
python_files = ["test_*.py"]
python_functions = ["test_*"]
addopts = [
    "--verbose",
    "--ruff",
    "--ruff-format",
    "--ty",
    "--cov=yaesm",
    "--cov-report=term-missing",
]

[tool.ty]
[tool.ty.src]
exclude = [".venv"]

[tool.ty.environment]
python-version = "3.10"

[[tool.ty.overrides]]
include = ["src/yaesm/driver/**"]

[tool.ty.overrides.rules]
invalid-method-override = "ignore"

[tool.coverage.run]
source = ["src"]
omit = ["*/tests/*"]

[tool.coverage.report]
exclude_lines = [
    "pragma: no cover",
    "def __repr__",
    "raise AssertionError",
    "raise NotImplementedError",
    "if __name__ == '__main__':",
]
